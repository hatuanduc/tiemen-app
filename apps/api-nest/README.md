Scaffolded Nest app for incremental migration of the existing Express API.

Next steps:
1. cd apps/api-nest
2. npm install
3. npm run start:dev

This scaffold includes a minimal Prisma provider and an Auth module to port from the existing API.